<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_tab_browser"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="edu.temple.webbrowser.TabBrowserActivity">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/urlAddress"
        android:id="@+id/frameWeb">

    </FrameLayout>

    <ImageButton
        android:src="@android:drawable/ic_media_previous"
        android:layout_width="35dp"
        android:layout_height="wrap_content"
        android:id="@+id/previousButton"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true" />

    <ImageButton
        android:src="@android:drawable/ic_media_next"
        android:layout_width="35dp"
        android:layout_height="wrap_content"
        android:id="@+id/nextButton"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@+id/previousButton" />

    <Button
        android:text="Go"
        android:layout_width="35dp"
        android:layout_height="wrap_content"
        android:id="@+id/goButton"
        android:layout_alignBottom="@+id/urlAddress"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@+id/urlAddress" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:hint="Type URL Here"
        android:ems="10"
        android:id="@+id/urlAddress"
        android:layout_alignBottom="@+id/nextButton"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@+id/nextButton" />

</RelativeLayout>

